import { wanderlustContracts } from './wanderlust-contracts';

describe('wanderlustContracts', () => {
  it('should work', () => {
    expect(wanderlustContracts()).toEqual('wanderlust-contracts');
  });
});
