// Stub - no resources yet. k8s resources for this stack go through
// @pulumi/kubernetes natively (never raw Helm/manifests, see
// feedback_pulumi_over_helm); provisioned starting iteration 7
// (Oracle A1 + k3s install). @pulumi/pulumi and @pulumi/kubernetes
// are already in package.json, ready for that iteration to import.
export {};
