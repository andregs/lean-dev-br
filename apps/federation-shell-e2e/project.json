{
  "name": "federation-shell-e2e",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/federation-shell-e2e/src",
  "tags": ["scope:federation", "type:e2e"],
  "implicitDependencies": ["federation-shell", "federation-catalog", "federation-cart"],
  "targets": {
    "e2e": {
      "executor": "@nx/playwright:playwright",
      "outputs": ["{projectRoot}/playwright-report", "{projectRoot}/test-results"],
      "options": {
        "config": "{projectRoot}/playwright.config.ts"
      },
      "configurations": {
        "ci": {
          "ci": true,
          "reporter": "line"
        }
      }
    }
  }
}
