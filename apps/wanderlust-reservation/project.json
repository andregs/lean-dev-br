{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "wanderlust-reservation",
  "projectType": "application",
  "sourceRoot": "apps/wanderlust-reservation/src",
  "tags": ["scope:wanderlust-reservation", "type:app", "lang:java"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/wanderlust-reservation",
        "command": "./mvnw -B -q clean package -DskipTests --no-transfer-progress"
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/wanderlust-reservation",
        "command": "./mvnw -B spring-boot:run --no-transfer-progress"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "cwd": "apps/wanderlust-reservation",
        "command": "./mvnw -B test --no-transfer-progress"
      }
    },
    "native-build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/wanderlust-reservation",
        "command": "./mvnw -B -Pnative native:compile --no-transfer-progress"
      }
    },
    "docker-build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/wanderlust-reservation",
        "command": "./mvnw -B -Pnative spring-boot:build-image -DskipTests --no-transfer-progress -Dspring-boot.build-image.imageName=${WANDERLUST_RESERVATION_IMAGE:-wanderlust-reservation:local} -Dspring-boot.build-image.env.BP_NATIVE_IMAGE_BUILD_ARGUMENTS=-J-Xmx5g"
      }
    },
    "test-integration": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/wanderlust-reservation",
        "command": "./mvnw -B failsafe:integration-test failsafe:verify --no-transfer-progress"
      }
    },
    "docker-push": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/wanderlust-reservation",
        "command": "docker push ${WANDERLUST_RESERVATION_IMAGE:?set WANDERLUST_RESERVATION_IMAGE to the registry image URL}"
      }
    }
  }
}
