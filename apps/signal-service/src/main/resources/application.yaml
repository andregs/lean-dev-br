spring:
  application:
    name: signal-service
  threads:
    virtual:
      enabled: true

relay:
  cors:
    allowed-origins: '*'
  room-ttl: 5m
  max-updates-per-room: 1000

logging:
  level:
    "[br.dev.lean.signal]": DEBUG
