spring:
  config:
    activate:
      on-profile:
        - prod
logging:
  structured:
    format:
      console: ecs # elastic common schema

relay:
  cors:
    allowed-origins: 'https://lean.dev.br'
