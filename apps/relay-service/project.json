{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "relay-service",
  "projectType": "application",
  "sourceRoot": "apps/relay-service/src",
  "tags": ["scope:relay", "type:app", "lang:java"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/relay-service",
        "command": "./mvnw -B -q clean package -DskipTests --no-transfer-progress"
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/relay-service",
        "command": "./mvnw -B spring-boot:run --no-transfer-progress"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "cwd": "apps/relay-service",
        "command": "./mvnw -B test --no-transfer-progress"
      }
    },
    "native-build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/relay-service",
        "command": "./mvnw -B -Pnative native:compile --no-transfer-progress"
      }
    },
    "docker-build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/relay-service",
        "command": "./mvnw -B -Pnative spring-boot:build-image -DskipTests --no-transfer-progress -Dspring-boot.build-image.imageName=${RELAY_IMAGE:-relay-service:local} -Dspring-boot.build-image.env.BP_NATIVE_IMAGE_BUILD_ARGUMENTS=-J-Xmx5g"
      }
    },
    "test-integration": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/relay-service",
        "command": "./mvnw -B failsafe:integration-test failsafe:verify --no-transfer-progress"
      }
    },
    "docker-push": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/relay-service",
        "command": "docker push ${RELAY_IMAGE:?set RELAY_IMAGE to the Artifact Registry image URL}"
      }
    }
  }
}
